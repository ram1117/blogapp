<div class="sign-up-form">

<h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="sign-field">
    <%= f.email_field :email, autofocus: true, autocomplete: "email",class:'sign-input-items', placeholder: 'Email' %>
  </div>

  <div class="sign-field">
    <%= f.password_field :password, autocomplete: "current-password",class:'sign-input-items', placeholder: 'Password'  %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="sign-field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="sign-actions">
    <%= f.submit "Log in",class:'sign-input-items'  %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</div>